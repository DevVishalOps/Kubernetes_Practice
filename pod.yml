apiVersion: V1
kind: Pod 
metadata: 
    name: pod_1
    labels:
      app: my-app 
      env: production 
    namespace: prod
spec: 
  containers:
    - name: my-nginx
      image: nginx
      ports:
        - name: http 
          containerPort: 80